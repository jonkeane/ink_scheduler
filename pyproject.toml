[project]
name = "ink_scheduler"
version = "0.1.0"
requires-python = ">= 3.10"
dependencies = [
    "shiny",
    "requests",
    "pandas",
    "numpy",
    "anthropic>=0.83.0",
    "openai>=2.22.0",
]
